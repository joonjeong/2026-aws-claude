[project]
name = "hub-infra"
version = "0.1.0"
description = "claude-lab hub — CDK stack (synth/deploy via uv)"
requires-python = ">=3.11"
dependencies = [
    "aws-cdk-lib>=2.150,<3",
    "constructs>=10.0,<11",
    "boto3>=1.34",  # synth 시 CloudFront origin-facing prefix list id 조회
]

[tool.uv]
package = false
